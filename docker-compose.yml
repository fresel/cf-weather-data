services:
  app:
    image: fresel72/cf-weather:latest
    container_name: cf-weather
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    expose:
      - "8080"
    environment:
      - WEATHER_API_KEY=${WEATHER_API_KEY}
